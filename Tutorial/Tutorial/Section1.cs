using System;
using System.Collections.Generic;
using System.Text;

namespace Tutorial
{
    class Section1
    {
        /*
         Section 1 - Intro, tools, 
            High Level into Assembly into Machine languages
            High level = no memory management
            Why C# = easy syntax, VS enhancement, compiler based (high to assembly in runtime)
         */
        public void PrintHello()
        {
            Console.WriteLine("S1 Hello World!");
        }
    }
}
